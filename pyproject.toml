[project]
name = "basketball-kings-agent"
version = "0.1.0"
description = "Daily NBA news briefing for the Basketball Kings show."
requires-python = ">=3.11"
dependencies = [
    "google-genai>=2.8.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
